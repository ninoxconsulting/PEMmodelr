% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/final_map.R
\name{final_map}
\alias{final_map}
\title{Combine the final map including forest/non-forest, forest and non-forest maps}
\usage{
final_map(
  fnf_map = fnf_map,
  fnf_key = fnf_key,
  f_map = f_map,
  f_key = f_key,
  nf_map = nf_map,
  nf_key = nf_key,
  out_dir = fs::path(PEMprepr::read_fid()$dir_3020_draft$path_rel),
  outname = "full_map.tif"
)
}
\arguments{
\item{fnf_map}{a character or path to the location of the forest/non-forest map (.tif)}

\item{fnf_key}{a character or path to the location of the forest/non-forest key}

\item{f_map}{a character or path to the location of the forest map (.tif)}

\item{f_key}{a character or path to the location of the forest key}

\item{nf_map}{a character or path to the location of the non-forest map (.tif)}

\item{nf_key}{a character or path to the location of the non-forest key}

\item{out_dir}{a character or path to the location of the output directory}

\item{outname}{a character with the name of the output map. Default is "full_map.tif"}
}
\value{
a SpatRaster object and response key saved to out_dir
}
\description{
Combine the final map including forest/non-forest, forest and non-forest maps
}
\examples{
\dontrun{
final_map( fnf_map = terra::rast(fs::path(fnf_dir, "best_map.tif")),
          fnf_key = utils::read.csv(fs::path(fnf_dir, "response_names.csv")),
          f_map <- terra::rast(fs::path(f_dir, "best_map.tif")),
          f_key <- read.csv(fs::path(f_dir, "response_key.csv")),
          nf_map <- terra::rast(fs::path(nf_dir, "best_map.tif")),
          nf_key <- read.csv(fs::path(nf_dir, "response_names.csv")),
          out_dir = fs::path(PEMprepr::read_fid()$dir_3020_draft$path_rel),
          outname = "full_map.tif")
}
}

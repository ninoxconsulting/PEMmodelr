% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_map.R
\name{predict_map}
\alias{predict_map}
\title{Generate maps from predicted model and output map or combined maps}
\usage{
predict_map(
  model = model,
  bec = bec,
  bec_shp = bec_shp,
  covars = covars,
  cov_dir = cov_dir,
  tile_dir = tile_dir,
  out_dir = out_dir,
  map_label = map_label,
  probability = FALSE
)
}
\arguments{
\item{model}{A fitted model object}

\item{bec}{A character defining the MAP_LABEL zone to be modeled, i.e. "ICHmc1"}

\item{bec_shp}{OPTIONAL: A sf object of the bec map, only needed for
model_type = "f". A sf object of the bec map}

\item{covars}{A character vector of the covariates to use}

\item{cov_dir}{A character defining the directory of the covariates}

\item{tile_dir}{A character defining the directory of the tiles}

\item{out_dir}{A character string with the directly where outputs will be saved.}

\item{map_label}{A character defining name of output, default is "final_map.tif"}

\item{probability}{A logical value indicating whether to output probability rasters. Default is FALSE.}
}
\value{
TRUE
}
\description{
Generate maps from predicted model and output map or combined maps
}
\examples{
\dontrun{
predict_map(rf_fit, out_dir, tile_dir, rstack, probability = FALSE)
}
}

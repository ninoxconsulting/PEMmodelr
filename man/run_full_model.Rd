% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_full_model.R
\name{run_full_model}
\alias{run_full_model}
\title{Run full model}
\usage{
run_full_model(
  model_name = NULL,
  bgc_pts_subzone,
  bec = bec,
  fuzz_matrix = NA,
  covars = covars,
  use_neighbours = FALSE,
  extra_pts = FALSE,
  extra_pts_ratio = 0.1,
  mtry = mtry,
  min_n = min_n,
  ntrees = 151,
  downsample_ratio = FALSE,
  smote_ratio = FALSE,
  nf_f_filter = FALSE,
  theta = c(0, 0.5, 1),
  report = FALSE,
  detailed_output = TRUE,
  out_dir = NA
)
}
\arguments{
\item{model_name}{A character string to define the model. Default will use balance combination}

\item{bgc_pts_subzone}{Datasets list with all formatted training data. Output of prep_model_tps()}

\item{bec}{A character with the BEC label to use. For example "ICHmc1".}

\item{fuzz_matrix}{data table with fuzzy metrics.}

\item{covars}{A vector with the names of covariates to use. These match raster names.}

\item{use_neighbours}{TRUE/FALSE. Define if you want to include all neighbours in the calculation}

\item{extra_pts}{logical. If TRUE, extra points will be included. Default is FALSE.}

\item{extra_pts_ratio}{numeric. If extra points are being used the ratio compared to the most common unit to which extra points will be added. The default is 0.1 or equivalent to 10\% of most common unit}

\item{mtry}{numeric. This is the output based on output of hyperparamter model tuning (default = ??)}

\item{min_n}{numeric. This is the output based on output of hyperparamter model tuning (default = ??)}

\item{ntrees}{numeric. Number of trees to use in random forest model. Default is 151.}

\item{downsample_ratio}{A vector of numeric downsampling values (10 - 100), NA if not using}

\item{smote_ratio}{A vector of numeric downsampling values (0.1 - 0.9), NA if not using}

\item{nf_f_filter}{A SpatRaster with binary forest (0) and nonforest (1)}

\item{theta}{numeric to decide what level of theta to report in the outputs. Default will produce theta (0,1)}

\item{report}{TRUE/FALSE: if you want to produce a pdf report summarising model inputs and model outputs}

\item{detailed_output}{OPTIONAL:TRUE/FALSE if you want to output all raw values
this is used to determine optimum theta values}

\item{out_dir}{OPTIONAL: only needed if detailed_output = TRUE. location of filepath there detailed outputs to be stored}
}
\value{
datatable of accuracy metric
}
\description{
Run full model
}
\examples{
\dontrun{
run_full_model(
 model_name = "base_model",
 bgc_pts_subzone = bgc_pts_subzone,
 bec = bec,
 fuzz_matrix = fuzz_matrix,
 covars = covars,
 use_neighbours = FALSE,
 extra_pts = FALSE,
 mtry = mtry,
 min_n = min_n,
 ntrees = 151,
 downsample_ratio = FALSE,
 smote_ratio = FALSE,
 nf_f_filter = nf_f_filter,
 heta = c(0,1),
 report = FALSE,
 detailed_output = TRUE,
 out_dir = out_bgc_dir
)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_tps.R
\name{prep_tps}
\alias{prep_tps}
\title{Prepare cleaned training points for model runs}
\usage{
prep_tps(
  allpts =
    sf::st_read(fs::path(PEMprepr::read_fid()$dir_20105030_attributed_field_data$path_rel,
    "allpoints_att.gpkg")),
  mapkey = utils::read.csv(fs::path(PEMprepr::read_fid()$dir_3010_inputs$path_rel,
    "mapunitkey_final.csv")),
  covarkey = utils::read.csv(fs::path(PEMprepr::read_fid()$dir_30_model$path_rel,
    "covar_key.csv")),
  attribute = "mapunit_ss_realm",
  extra_pts = FALSE,
  bec = sf::st_read(fs::path(PEMprepr::read_fid()$dir_1010_vector$path_rel, "bec.gpkg")),
  min_no = 10
)
}
\arguments{
\item{allpts}{A \code{sf} object with cleaned and attribute points. This is the output of \code{PEMsamplr::attribute_pts()}}

\item{mapkey}{A csv with map unit key. This is the output of \code{PEMsamplr::generate_mapkey()}}

\item{covarkey}{A csv with covariate key. This is the output of \code{PEMmodelr::generate_covar_key()}}

\item{attribute}{A character string matching the columns within the mapkey which fieldmap units will be matched to. The default
is "mapunit_ss_realm" for forest model, and "mapunit_fnf" for forest non-forest model, and "mapunit_fnf" for non-forest model.}

\item{extra_pts}{A Logical TRUE/FALSE to include extra points in the training dataset. Default is FALSE.}

\item{bec}{A \code{sf} object with BEC zones. The default points to the BEC zones .gpkg in the standard workflow.}

\item{min_no}{A numeric value of the minimum number of points required for each map unit. Default is 10.}
}
\value{
A cleaned training point dataset with BEC zones and map units assigned.
}
\description{
Prepare cleaned training points for model runs
}
\examples{
\dontrun{
tpts <- prep_tps(
  allpts = sf::st_read(
    fs::path(PEMprepr::read_fid()$dir_20105030_attributed_field_data$path_rel,
    "allpoints_att.gpkg")),
  mapkey = read.csv(fs::path(PEMprepr::read_fid()$dir_3010_inputs$path_rel,
  "mapunitkey_final.csv")),
  covarkey = covarkey,
  attribute = "mapunit_ss_realm",
  extra_pts = FALSE,
  bec = sf::st_read(fs::path(PEMprepr::read_fid()$dir_1010_vector$path_rel, "bec.gpkg")),
  min_no = 20
)
}
}
